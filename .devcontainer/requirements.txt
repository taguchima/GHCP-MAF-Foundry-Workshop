# Devcontainer 内で事前インストールする Python パッケージ
# (docs/00-setup.md / docs/04-trace-evaluation.md と一致させる)
# `--pre` は agent-framework-foundry のプレリリース版を有効にするための pip グローバル オプション
--pre

agent-framework-foundry
azure-identity
python-dotenv
pydantic
pytest
azure-ai-projects>=2.1.0
